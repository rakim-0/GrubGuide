<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>GrubGuide</title>
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
            rel="stylesheet"
        />
    </head>
    <style>
        body {
            text-align: center;
            /* background-image: url(https://safe.manu.moe/gIuVHmY5.png); */
            background-size: 100% 120%;
        }

        h1 {
            font-family: helvetica;
            margin-top: 5rem;
            font-size: 6rem;
        }

        .search-container form {
            display: flex;
        }

        .search-container input {
            margin-right: 10px;
        }
    </style>

    <body class="d-flex flex-column min-vh-100">
        <%- include('partials/navbar') %>
        <main class="flex-grow-1 pt-5">
            <h1>GrubGuide</h1>
            <% if (locals.currentUser) {%>
                <h2>Hi, <%= currentUser.name%></h2> 
                <div id="userid" style="color: white;"><%= currentUser.id%></div>
            <% } %>         
            <h2>Find Your Next Favorite Meal!</h2>
            <div class="search-container d-flex justify-content-center">
                <!-- <div class="search-container pt-3">
                    <form action="#">
                        <input class="form-control" id="inlineFormInput" type="text"
                            placeholder="Enter your location..." name="search">
                        <button type="submit" class="btn btn-dark">Submit</button>
                    </form>
                </div> -->
            </div>
        </main>
        <div class="row flex-grow-1 justify-content-center pt-3">
            <div class="col-sm-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Order</h5>
                        <p class="card-text">Delicious Meals at Your Doorstep.</p>
                        <a href="all-dishes" class="btn btn-dark">Order Now</a>
                    </div>
                </div>
            </div>
            <div class="col-sm-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Dining</h5>
                        <p class="card-text">View city's favorite dining venues.</p>
                        <a href="/all-restaurants" class="btn btn-dark">Explore</a>
                    </div>
                </div>
            </div>
        </div>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
        <%- include('partials/footer') %>
    </body>
    <script src="user.js"></script>
</html>
